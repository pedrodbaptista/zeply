import * as userModel from "./userReducer/userModel";
import * as blockchainModel from "./blockchainReducer/blockchainModel";

const models = {
  user: userModel,
  blockchain: blockchainModel,
};

export default models;
